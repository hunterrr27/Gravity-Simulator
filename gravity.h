#ifndef GRAVITY_H
#define GRAVITY_H

#include <iostream>
#include <vector>
#include <string>
#include "raylib.h"
#include <time.h>

#define G 500.0f

/*
class Ball {
public:
    // characteristics
    float mass;
    float velocity;
    float radius;

    // position
    float x;
    float y;

    // constructor
    Ball(float startX, float startY) : x(startX), y(startY) {}

    // methods
    // draw method
    void Draw();
    void Update(float deltaTime);
    void Bounce(float floorY);
    float GetY();
    float GetRadius();

};
*/

#endif // !GRAVITY_H
